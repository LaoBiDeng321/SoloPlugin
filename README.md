# 本地服务插件管理器

一个功能完善的浏览器插件，用于在访问本地Web服务时自动管理其他浏览器插件的状态。

## 功能特性

### 核心功能

- **自动检测本地服务**：当浏览器访问用户配置列表中的本地服务（包括localhost及其他本机Web服务）时，自动检测并禁用除本插件外的所有其他已安装浏览器插件
- **智能恢复插件**：当用户离开这些本地服务页面（包括关闭标签页、导航至非列表中地址或关闭浏览器）时，精确恢复之前禁用的所有插件至原始状态
- **无干扰运行**：确保在插件禁用/恢复过程中不影响目标本地Web服务的正常加载、渲染及功能使用

### 用户配置管理

- **友好的配置界面**：提供直观的配置界面，允许用户添加、编辑、删除需要应用插件禁用规则的本地服务域名或IP地址列表
- **灵活的匹配模式**：支持通配符匹配或正则表达式功能，以灵活匹配本地服务地址模式
- **开关控制**：提供开关控件允许用户临时启用/禁用本插件的自动管理功能

### 用户界面

- **状态指示器**：设计直观的插件状态指示器，清晰显示当前是否处于插件禁用模式
- **插件管理面板**：实现详细的插件管理面板，展示当前所有浏览器插件的状态信息
- **手动控制**：提供简洁的操作界面，允许用户手动触发插件禁用/恢复操作

### 兼容性与性能

- **浏览器兼容**：兼容主流现代浏览器，包括Google Chrome（版本88+）和Microsoft Edge（版本88+）
- **高性能**：优化性能，确保插件检测和状态切换操作响应时间不超过300ms
- **可靠的状态保存**：实现可靠的状态保存机制，确保浏览器意外关闭后仍能恢复插件原始状态

### 主题支持

- **暗色主题**：使用暗色半透明毛玻璃质感的卡片和背景，配合白色文本
- **亮色主题**：使用亮色半透明毛玻璃质感的卡片和背景，配合黑色文本

## 安装方法

### 开发者模式安装

1. 下载或克隆本项目到本地
2. 打开Chrome或Edge浏览器
3. 访问 `chrome://extensions/`（Chrome）或 `edge://extensions/`（Edge）
4. 开启右上角的"开发者模式"
5. 点击"加载已解压的扩展程序"
6. 选择项目根目录
7. 插件安装完成

### 打包安装

1. 在浏览器扩展管理页面点击"打包扩展程序"
2. 选择项目根目录
3. 生成.crx或.zip文件
4. 将文件拖拽到浏览器中安装

## 使用说明

### 基本使用

1. 安装插件后，点击浏览器工具栏中的插件图标打开弹出窗口
2. 在弹出窗口中可以看到当前的插件状态和已禁用的插件数量
3. 点击"设置"按钮进入详细配置页面

### 配置监控域名

1. 在设置页面的"监控域名"部分，输入要监控的域名或IP地址
2. 点击"添加"按钮将域名添加到列表中
3. 支持通配符匹配，如 `*.localhost` 或 `192.168.*`
4. 如需使用正则表达式，开启"使用正则表达式"开关

### 启用/禁用自动管理

1. 在弹出窗口或设置页面中，找到"启用自动管理"开关
2. 开启后，插件将自动管理访问本地服务时的其他插件状态
3. 关闭后，插件将暂停自动管理功能

### 手动控制插件

1. 在弹出窗口中，点击"禁用其他插件"按钮可手动禁用所有其他插件
2. 点击"恢复所有插件"按钮可恢复所有被禁用的插件
3. 适用于需要临时控制插件状态的场景

### 主题切换

1. 在设置页面的"主题设置"部分，选择"亮色主题"或"暗色主题"
2. 主题设置会自动保存并在下次使用时应用

## 项目结构

```
.
├── manifest.json          # 插件配置文件
├── popup.html             # 弹出窗口页面
├── options.html           # 设置页面
├── css/
│   ├── popup.css          # 弹出窗口样式
│   └── options.css        # 设置页面样式
├── js/
│   ├── background.js      # 后台服务脚本
│   ├── popup.js           # 弹出窗口逻辑
│   └── options.js         # 设置页面逻辑
├── svg/
│   ├── icons.svg          # 图标集合
│   ├── icon-16.svg        # 16x16图标
│   ├── icon-32.svg        # 32x32图标
│   ├── icon-48.svg        # 48x48图标
│   └── icon-128.svg       # 128x128图标
└── README.md              # 项目文档
```

## 技术实现

### Manifest V3

本插件使用Chrome Extension Manifest V3规范开发，确保与最新浏览器标准兼容。

### 核心API

- **chrome.management**：用于获取和管理浏览器插件状态
- **chrome.storage**：用于保存用户配置和插件状态
- **chrome.tabs**：用于监听标签页变化事件

### 性能优化

- 异步操作确保UI不阻塞
- 批量操作减少API调用次数
- 状态缓存减少重复查询

## 安全性

- 仅请求必要的权限
- 不收集或上传任何用户数据
- 所有配置数据保存在本地
- 遵循浏览器扩展安全最佳实践

## 浏览器兼容性

- Google Chrome 88+
- Microsoft Edge 88+
- 其他基于Chromium的浏览器

## 开发说明

### 开发环境

- Node.js（可选，用于构建工具）
- Chrome或Edge浏览器
- 代码编辑器（如VS Code）

### 调试方法

1. 在浏览器扩展管理页面找到本插件
2. 点击"检查视图"查看后台服务日志
3. 在弹出窗口或设置页面右键选择"检查"查看前端日志

### 构建发布

1. 检查代码质量和功能完整性
2. 更新版本号
3. 打包扩展程序
4. 提交到Chrome Web Store或Microsoft Edge Add-ons

## 常见问题

### Q: 插件无法禁用某些插件？

A: 某些浏览器内置插件或系统插件可能无法被禁用，这是浏览器的限制。

### Q: 如何备份我的配置？

A: 配置数据保存在浏览器的本地存储中，可以通过浏览器同步功能进行备份。

### Q: 插件会影响浏览器性能吗？

A: 本插件经过性能优化，正常使用下对浏览器性能影响极小。

### Q: 支持哪些本地服务地址？

A: 支持所有本地Web服务地址，包括localhost、127.0.0.1、0.0.0.0、::1以及自定义的本地域名。

## 更新日志

### v1.0.2 (2026-02-22)

- 添加常见本地开发服务端口（3000, 3001, 4000, 5000, 5500, 8000, 8080, 9000, 4200, 5173）
- 支持localhost和127.0.0.1的常见开发端口自动管理

### v1.0.1 (2026-02-22)

- 修复插件类型过滤问题，现在会尝试禁用所有类型的插件
- 优化错误处理，确保插件禁用/恢复操作更加稳定

### v1.0.0 (2026-02-22)

- 初始版本发布
- 实现核心插件管理功能
- 支持通配符和正则表达式匹配
- 提供暗色和亮色主题
- 完整的用户配置界面

## 许可证

MIT License

## 贡献

欢迎提交Issue和Pull Request来改进本项目。

## 联系方式

如有问题或建议，请通过GitHub Issues联系。